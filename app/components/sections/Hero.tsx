/**
 * Hero section — placeholder.
 * Real copy + layout/scroll animations land in a follow-up PR.
 */
export function Hero() {
  return (
    <section id="hero" className="flex min-h-[60vh] items-center justify-center">
      <p className="text-foreground/50">Hero placeholder</p>
    </section>
  );
}
